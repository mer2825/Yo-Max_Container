# ===================================================================
# CONFIGURACIÓN DE BASE DE DATOS FLEXIBLE (LOCAL Y PRODUCCIÓN)
# ===================================================================
# Si DATABASE_URL está presente (en Railway), se usará esa.
# Si no, se usarán las variables locales (para desarrollo).
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/yomax}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Flames1902}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================================================================
# CONFIGURACIÓN DE JPA E HIBERNATE
# ===================================================================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=${SQL_INIT_MODE:never}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.open-in-view=true

# Configuración para aceptar Content-Type con charset
spring.mvc.contentnegotiation.favor-parameter=false
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# ===================================================================
# SERIALIZACIÓN JSON - Fechas como strings ISO-8601
# ===================================================================
spring.jackson.serialization.write-dates-as-timestamps=false

# ===================================================================
# OTRAS CONFIGURACIONES DE LA APLICACIÓN
# ===================================================================
# Puerto dinámico para producción, 8080 para local
server.port=${PORT:8080}

# Directorio de subida de archivos (legado, migrando a Cloudinary)
file.upload-dir=${FILE_UPLOAD_DIR:./src/main/resources/static/uploads/}

# Desactivar persistencia de sesión para evitar errores con DevTools
server.servlet.session.persistent=false

# ===================================================================
# CREDENCIALES Y CONFIGURACIÓN DE ENTORNO
# ===================================================================
# Carpeta base en Cloudinary. 'desarrollo' por defecto.
# En producción (Railway), se sobreescribe a 'produccion'.
cloudinary.folder.env=${CLOUDINARY_FOLDER_ENV:desarrollo_Oscar}

# Google reCAPTCHA v2
google.recaptcha.key.site=${RECAPTCHA_SITE_KEY:6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI}
google.recaptcha.key.secret=${RECAPTCHA_SECRET_KEY:6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe}
google.recaptcha.verify.url=${RECAPTCHA_VERIFY_URL:https://www.google.com/recaptcha/api/siteverify}
google.recaptcha.enabled=${RECAPTCHA_ENABLED:false}

# API Token para servicio externo
api.external.token=${API_EXTERNAL_TOKEN:un_token_seguro_aqui}

# Proveedor de facturación electrónica
facturacion.provider=${FACTURACION_PROVIDER:miapi}

# APISUNAT (respaldo / compatibilidad)
apisunat.url=${APISUNAT_URL:https://back.apisunat.com}
apisunat.token=${APISUNAT_TOKEN:DEV_naz8BIm1oDrQh0qqMlsNr6NiGdRblW9aI1KIMo01GSKSLcJrLYECYBAiN8nsjEA7}
apisunat.persona-id=${APISUNAT_PERSONA_ID:6a39bbb50154ec0029769a4a}
apisunat.persona-token=${APISUNAT_PERSONA_TOKEN:DEV_naz8BIm1oDrQh0qqMlsNr6NiGdRblW9aI1KIMo01GSKSLcJrLYECYBAiN8nsjEA7}
apisunat.ruc=${APISUNAT_RUC:20556548745}
apisunat.documents-uri=${APISUNAT_DOCUMENTS_URI:/personas/v1/sendBill}

# MiAPI Cloud (activo por defecto para emitir boletas/facturas/notas)
miapi.base-url=${MIAPI_BASE_URL:https://miapi.cloud}
miapi.token=${MIAPI_TOKEN:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjozNDYsImV4cCI6MTc2MDM3Njc5NH0.Ow-U3j-J2A2cIV5Z_X9lKs-kHGasITmsD9aDhGPm37Q}
miapi.secret-key=${MIAPI_SECRET_KEY:miap-r4a-t9v-35e}
miapi.credit-note-uri=${MIAPI_CREDIT_NOTE_URI:/apifact/note/create}

# ===================================================================
# CONFIGURACIÓN DE EMAIL (SMTP - MailerSend)
# ===================================================================
# MailerSend SMTP configurado para envío de reportes de cierre
spring.mail.host=${MAIL_HOST:smtp.mailersend.net}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:MS_LX3BG1@test-zkq340e7v62gd796.mlsender.net}
spring.mail.password=${MAIL_PASSWORD:mssp.urwUUGQ.3z0vklo9767l7qrx.KdCR1bF}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.mailersend.net
spring.mail.from=${MAIL_FROM:MS_LX3BG1@test-zkq340e7v62gd796.mlsender.net}

# ===================================================================
# CREDENCIALES DE CLOUDINARY
# ===================================================================
cloudinary.cloud_name=your_cloud_name
cloudinary.api_key=your_api_key
cloudinary.api_secret=your_api_secret